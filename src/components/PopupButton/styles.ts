import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles((theme: any) => ({
  card: {
    //padding: theme.spacing(1),
    width: 'auto',
    height: 'auto'
  },
  // popOver: {
  //     boxShadow: "0px 2px 1px -3px rgba(0,0,0,0.2), 0px 1px 1px 1px rgba(0,0,0,0.14), 0px 1px 4px 1px rgba(0,0,0,0.12)"
  // }
}));

export default useStyles;